# Web demo dependencies only (no desktop GUI).
# Used by the Docker image so the container stays slim — customtkinter and its
# native Tk dependencies are intentionally excluded.
python-dotenv==1.0.1
groq>=0.11.0
httpx==0.27.2
Flask>=3.0.0
gunicorn>=21.2.0
Flask-Limiter>=3.5.0
